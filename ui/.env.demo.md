# Used by `npm run build:demo` (vite --mode demo) to build the public,
# static GitHub Pages demo. No backend, no auth issuer: the app runs
# entirely on in-browser mock data (see src/api/demoClient.ts).
VITE_DEMO_MODE=true